---
name: project-malc-paper
description: MAL-C dual-track routing/labelling paper (mal_c_simplified_en.tex) — reviewed defects and counterexamples
metadata:
  type: project
---

Paper: `/home/nsdlab/student/114/114753109/paper/mal_c_simplified_en.tex`. MAL-C = Minimum Aged Labelling with Capacity. Dual-track: SP for integer/leftover flow, DSF backbone for fractional flow consolidated via Assign-Dual-Labels. Claimed ratio O(√h n^{1/3}+n^{2/3}).

**Why:** User wants hostile correctness review of algorithm + proofs + approximation analysis.

**How to apply:** Key fatal-defect candidates found:
- **Shortest-path arborescence existence inside SCC (FATAL):** in-tree/out-tree built with travel times w must be SPANNING arborescences within E_SF, but E_SF (a DSF *approximation*) restricted to an SCC need not contain a directed spanning tree to/from an arbitrary center h_i. Strong connectivity of the contracted SCC does NOT guarantee the sub-edges in E_SF form spanning in/out trees rooted at h_i; canonical route may use edges not in E_SF.
- **OPT_frac not a valid lower bound:** OPT_frac = Σ dem·len(SP) is defined as a *cost* (sum over shortest-path edges of un-rounded flow). But real OPT rounds up per (edge,time). Claim OPT≥OPT_frac needs that fractional relaxation lower-bounds rounded objective AND that shortest paths are optimal routing for the relaxation — plausible but SP-forcing in the relaxation is an assumption, and OPT_frac counts each unit flow×path-length which can EXCEED true fractional optimum if consolidation across demands reduces (edge,time) load. Actually direction matters: need OPT ≥ OPT_frac; since removing round-up only lowers cost and SP minimizes path length, OPT_frac ≤ OPT holds only if OPT also must pay ≥ flow·pathlen — true because each unit must traverse ≥ len(SP) edges and round-up ≥ actual flow. Considered OK.
- **len(SP) integer-flow bound eq (7):** Σ d_I·len(SP) ≤ OPT_frac fine.
- **Lemma 2 Case "a reached via in-tree":** canonical route always exits via out-tree to cross tail, so in-tree-arrival case is vacuous/redundant but harmless.
- Two-label claim: cross vs tree edge mutual exclusivity assumed; a cross edge is between components so cannot be a tree edge (trees are intra-component) — OK. But an intra edge could be in-tree in C_i AND also... only one component, fine.
- **All three temporal claims collapse to the same root gap:** (a) Lemma 2 needs cross-tail a to have d_out(a) defined (a on out-tree); (b) Thm induction needs every entry node q on in-tree with d_in(q) defined; (c) Lemma 1 needs both u,v on the two trees. All require the two shortest-path arborescences to be SPANNING *using only E_SF edges*. This is the single FATAL defect.
- **Approximation algebra all CHECKS OUT (verified by hand):** theta*=1/(√h n^{1/3}); at opt both theta-terms=√h n^{1/3}; crossover h=n^{2/3}; theta*<1 essentially always. Remark on infrastructure term 2|E_SF| retention is correct and honest. Do NOT flag these as errors.
- **OPT>=OPT_frac direction OK** (round-up>=actual, each unit traverses >=len(SP) edges). **OPT>=OPT_SF OK** (feasible soln's support contains a DSF). eq(7) integer bound, eq(9) x/theta>1 bound both fine.
- Minor/non-fatal writeup gaps to still mention: Lemma 2 Case-2 (a via in-tree) is vacuous under canonical route (exit always via out-tree) — redundant not wrong; w:E->N allows w(e)=0 self-loops/zero-weight edges, unaddressed but harmless; Route-SP induced labels differ per demand on shared edges — SP analysis assumes worst-case no-sharing so it's a safe over-count, fine but under-explained.

Round 1 verdict: FAIL — single fatal defect = spanning-arborescence-within-E_SF gap (Rooted-trees paragraph + Lemmas 1,2 + Thm temporal). Everything else (approx analysis, lower bounds, two-label counting) is sound.

**Round 2 (2026-07-25): PASS.** BUG #1 fully fixed. Author now defines E_SF[C_i]=induced subgraph of E_SF on C_i's vertices and argues it is strongly connected. This is a VALID standard theorem: for an SCC, all mutual-reachability paths stay inside the vertex set (any detour vertex is itself mutually reachable ⇒ in the SCC by maximality), so induced subgraph is strongly connected using only E_SF edges ⇒ in/out shortest-path arborescences span C_i with E_SF edges only. Recurrences d_in(x)=w+d_in(x'), d_out(y')=d_out(y)+w now rigorously grounded. Re-verified all new changes: Route-SP ℓ_k formula + a→b→c example (no off-by-one), TotalRatio(θ) def matches eq:sp+eq:sf three-term-for-three-term, θ* algebra, no dangling CDO25 refs (only BBMRY13 remains, defined).
Remaining NON-FATAL writeup nits (do not affect correctness): (1) Thm1 induction invariant stated as "entry reached by T_{i_l}" but source base case only gives arrival AT its in-tree label (≥T_{i_1}); real working invariant is "arrival ≤ entry's in-tree departure label" — machine still valid since Lemma1's true precondition is met everywhere. (2) Lemma2 in-tree-arrival case for cross-tail a is vacuous (canonical route exits via out-tree) — redundant not wrong. (3) |R_i|≤|E_SF| in eq:flowterm ignores possible edge reuse in ascent+descent, at most a constant factor, O() absorbs. (4) w:E→ℕ may include w=0; harmless (equalities stay time-respecting, +1 in base-time DP guarantees inter-component progress).

**Round 3 (2026-07-30): FAIL — new bug introduced by Change 1.** Author rewrote Definition 1 (Temporal path) and pinned the label domain to ℕ={1,2,3,...} (line 201), i.e. labels must be ≥1, EXCLUDING 0. But (a) Route-SP explicitly assigns label 0 to the first edge (freight leaves source at time 0; ℓ_1=empty sum=0, lines 138-139), and (b) in C_1 with T_1=0, dual-labels also produce 0 — e.g. 2-cycle h⇄x, w=1: in-tree edge (x,h) gets t_in=T_1+D_in−d_in(x)=0+1−1=0. So the algorithm's own labels (label 0) fall outside λ(e)⊆2^ℕ as Def 1 now defines ℕ, and violate feasibility condition (ii) (t∈λ(e) with t∈ℕ={1,2,...}). Fix direction: define ℕ={0,1,2,...}/ℕ_0 for the label domain, or start clock at 1. This is a genuine internal inconsistency, not just a nit — the domain declaration and the constructed output contradict.
Change 2 (Lemma 2 out-tree-only) is CORRECT: canonical route (Def 2) exits every component via out-tree descent to the exit=cross-tail a, so arrival T_i+D_in+d_out(a) ≤ T_i+D_in+D_out=t_cross always holds; boundary cases a=h_i (d_out=0), entry=exit=h_i all satisfied (freight waits to t_cross). Deleting in-tree case is fine (was vacuous). Change 3 (fig:cross) is consistent with Lemma 2 text/formulas (t_cross, T_j≥t_cross+w, in-tree ≥T_j). Round-2 fatal fix (E_SF[C_i] induced strongly connected ⇒ spanning arborescences) still intact. Approx analysis, lower bounds, two-label counting all still sound. N1/N3/N4 nits persist, still non-fatal. Only blocker = label-0 vs ℕ={1,2,...} domain clash.

**Round 4 (2026-08-04): PASS.** Round-3 label-domain bug FIXED: domain now uniformly λ:E→2^{ℤ_{≥0}}, ℤ_{≥0}={0,1,2,…} in Output (lines 90/96) and Def 1 (line 199). Route-SP label 0 and T_1=0-derived label 0 now legal. Only remaining ℕ is w:E→ℕ (line 82, TRAVEL TIMES only — no clash; nothing requires w=0). Author's new worked example (§3.4 "A worked example: assigning the labels" + fig:example): TWO directed triangles C_1: a→b→c→a (w=2,1,3), C_2: p→q→r→p (w=1,1,1), cross c→q (w=2). INDEPENDENTLY RECOMPUTED EVERY NUMBER — all 7 checkpoints correct:
(1) topo order C_1≺C_2, cross=c→q ✓. (2) C_1 center a: in-tree b→c→a d_in(a)=0,d_in(c)=3,d_in(b)=4; out-tree a→b→c d_out(a)=0,d_out(b)=2,d_out(c)=3; D_in=4,D_out=3; labels (b,c)_in=0,(c,a)_in=1,(a,b)_out=4,(b,c)_out=6; (b,c) on BOTH trees={0,6} ✓. (3) intra c→b route c→a→b: (c,a)@1→(a,b)@4, arrive a@4=T_1+D_in, b@6; each dep≥prev arr ✓. (4) t_cross(c,q)=0+4+3=7; T_2=max{1,7+2}=9 ✓. (5) C_2 center p: in-tree q→r→p d=0,1,2; out-tree p→q→r d=0,1,2; D_in=D_out=2; labels (q,r)_in=9,(r,p)_in=10,(p,q)_out=11,(q,r)_out=12; (q,r)={9,12} ✓. (6) inter a→r route a→b→c→(cross)→q→r→p→q→r: (a,b)@4→(b,c)@6→(c,q)@7→(q,r)@9→(r,p)@10→(p,q)@11→(q,r)@12, arrive r@13; every dep≥prev arr; (c,q)@7→q@9=T_2 ✓; (r,p)@10→p@11=T_2+D_in^(2) ✓. Note route reuses edge (q,r) at both its labels 9 (in-role) & 12 (out-role) — legal temporal WALK not path, consistent. (7) fig:example numbers exactly match: C_1 (a,b)=4,(b,c)={0,6},(c,a)=1; C_2 (p,q)=11,(q,r)={9,12},(r,p)=10; cross=7; caption correct. No dangling refs (only BBMRY13). N1/N3/N4 still non-fatal writeup nits (N3 |R_i|≤|E_SF| edge-reuse now VISIBLE in example a→r reusing (q,r), still O()-absorbed). No new bug. VERDICT PASS.

**Round 5 (2026-08-08): PASS — full restructured file `mal_c_restructured.tex` (definition-before-use reordering).** Re-verified all 6 announced changes:
1. eq:basetime expanded to T_i=max{T_{i-1}+1, max_{cross (x,y),x∈C_j,y∈C_i,j<i}(T_j+D_in^(j)+D_out^(j)+w(x,y))}. EXACT: t_cross depends only on tail component so T_j+D_in^(j)+D_out^(j)=t_cross(x,y); term=t_cross+w. No fwd-ref cycle (D_in/D_out for all comps computed before base times, Alg2 lines2-4 before line5). Lemma2's T_j≥t_cross(c)+w(c) still holds (index convention flips i↔j between eq:basetime and Lemma2 but substitution checks out). Worked ex T_2=max{1,0+4+3+2}=9 ✓.
2. Canonical route now independent Definition 2 (in/out-tree description); Route-SF defined FROM it. No circularity. Lemmas/Thms use it consistently (exit=cross-tail reached by out-tree descent). ✓
3. eq:treerec: d_in(x)=w(x,x')+d_in(x') [in-tree], d_out(y')=d_out(y)+w(y,y') [out-tree]. Both correct (x=tail farther, x'=parent closer / y=parent, y'=child). Lemma1 ascent+descent label arithmetic verified exact. ✓
4. SF constant: eq:flowterm |R_i|≤2|E_SF|, Σ_e F_e≤2θ·h·2|E_SF|=4hθ|E_SF|; eq:sf ALG_SF≤(4hθ+2)|E_SF|=O((hθ+1)n^{2/3})OPT. THE ONE NOTABLE NIT: F_e DEFINED (line607) as Σ_{i:e∈R_i}s_i = SET MEMBERSHIP (no multiplicity), but eq:sfsum's "f_{t1}+f_{t2}=F_e" and eq:flowterm's "Σ_e F_e=Σ_i s_i|R_i|" require the WITH-MULTIPLICITY version F'_e (a single route double-uses an edge when it's on both in- and out-tree — the paper's OWN a→r example reuses (q,r) at labels 9 AND 12). So "f_{t1}+f_{t2}=F_e" is literally FALSE on that example (LHS=F'_e>F_e). BUT: error direction is favorable — the chain actually uses F'_e (=f_{t1}+f_{t2}), and Σ_e F'_e=Σ_i s_i|R_i|_mult is exactly what eq:flowterm bounds ("with multiplicity" is even written on line614). So final bound stands. Also |R_i|≤2|E_SF| undercounts cross edges (true clean bound 3|E_SF|), O()-absorbed. Both = NARRATION/NOTATION defects, NOT theorem-breaking. Classify as "needs fix for rigor, doesn't affect correctness."
5. eq:spfrac reason: x_i=d_f−θ<dem(d_i) & subset {d_f>2θ}⊆all ⟹ Σx_i len(SP_i)≤OPT_frac. Correct. Earlier step 1<x_i/θ from d_f>2θ⟹x_i>θ. Correct.

**Round 6 (2026-08-09): PASS — three notation fixes + writing edits, all verified.** File `mal_c_restructured.tex`.
FIX 1 (F_e multiplicity, the ONE Round-5 nit) is now FIXED: line 610 F_e=f_{t1}(e)+f_{t2}(e) "total flow dispatched on e across its labels" — a per-label-bucket sum, i.e. WITH multiplicity. Recomputed a→r example: edge (q,r) F_e=0.6=0.3+0.3 (counts demand in BOTH label buckets 9&12), Σ_e F_e=2.1=s·|R|=0.3·7. eq:flowterm Σ_e F_e=Σ_i s_i|R_i| now LITERALLY TRUE (previously false on this example); |R_i| also with-multiplicity (line 619). eq:sfsum ⌈f_t1⌉+⌈f_t2⌉≤F_e+2 holds unconditionally. Final ALG_SF≤(4hθ+2)|E_SF|=O((hθ+1)n^{2/3}) intact.
FIX 2 (|R_i|≤2|E_SF|, "each edge at most twice: in-tree once + out-tree once + cross once", lines 617-619) VALID: canonical route visits each component ≤once (strict topo order, DAG), so an intra edge used ≤2 (in+out role in its single visit), cross ≤1. Airtight. (Prior "clean bound 3|E_SF|" undercounting nit N3 now RESOLVED by this tighter argument.)
FIX 3 (Rooted-trees line 235-236 "Because C_i is an SCC of G_SF, the subgraph it induces is strongly connected") VALID standard theorem (induced subgraph of an SCC is strongly connected — any detour vertex is mutually reachable ⇒ in C_i by maximality). Grounds spanning in/out arborescences using only E_SF edges. This is the compact restatement of the Round-2 fatal fix; still intact.
Point 3 base-time DP vs Lemma2: index i↔j flip between eq:basetime (receiving=C_i) and Lemma2 (tail=C_i); substitution gives the exact term inside eq:basetime's max ⇒ Lemma2's T_j≥t_cross+w is a direct consequence. CONSISTENT. Thm1 induction, Thm2 two-label, canonical route Def2, eq:treerec all self-consistent.
Point 4 full chain (OPT≥OPT_frac, OPT≥OPT_SF, SP (1+1/θ)OPT_frac, SF, θ*=1/(√h n^{1/3}), two regimes, common-pitfall hθ*<1 when h<n^{2/3}) all re-verified.
Point 5 worked example: ALL numbers recomputed independently — C_1 D_in=4,D_out=3, (b,c)={0,6},(a,b)=4,(c,a)=1; intra c→b b@6; t_cross=7; T_2=9; C_2 (q,r)={9,12},(p,q)=11,(r,p)=10; inter a→r monotone, r@13, cross→q@9=T_2. Figure labels match text. ALL CORRECT.
Point 6 refs: programmatic audit — 45 refs all resolve (no ??), only \cite=BBMRY13 defined. §1.3 "(formally, the objective (1))" is a benign forward pointer to eq:objective defined a few lines later in the same Problem 2 block — NOT dangling.
NEW writing nit (non-fatal, not a correctness bug): line 110 "MAL, introduced by Carnevale et al." is a prose attribution with NO \cite and NO bibliography entry — uncredited reference. Compiles fine (not a \cite so no ??), but bibliography-completeness gap. Author should add the Carnevale et al. reference. This is the ONLY outstanding item; purely editorial.
VERDICT PASS. All three notation fixes correct; the last remaining Round-5 rigor nit (F_e multiplicity) is now fully closed.
**Round 7 (2026-09-06): PASS — writing/definition-presentation batch (advisor comments) on `mal_c_restructured.tex`.** Six changes, all verified:
1. §1 "recalls"→"introduces the labelling problems" — pure wording, fine.
2. Base problem changed MAL→ML (Klobas et al. KMMS24): Problem 1 now Input=G only, Output λ:E→2^{Z≥0}, Constraint temporally connected every ordered pair, Objective min Σ|λ(e)|. Matches Klobas ML. Prose line110-113 correct (ML=fewest labels; MAL=ML+largest-label bound [CDO25]; our problem builds on ML, no label bound). Label renamed prob:mal→prob:ml — NO dangling \ref (grep: prob:mal absent, prob:ml defined line115, all refs resolve). NO residual {1,…,a} / max-label-bound a anywhere (only mention is correct "imposes no maximum-label bound", line135). Clean.
3. MAL-C constraint (ii) dropped "all flow released at time 0" from the enumerate; time-0 release NOW carried by model prose line136 "all demands are available from time 0". Route-SP (line310) and T_1=0 still grounded — the model setting survives. Fine.
4. §3 opening meta-sentence "Because each ingredient is defined before it is used…" DELETED. Definitions still in fact ordered before use; removing prose introduces no gap. Fine.
5. Lemma2 + fig:cross cross-edge var c→e (e=(a,b), t_cross(e), w(e)). NO collision: every other `e` (§3.2 generic e=(x,y), Alg, Thm2, e_k in Route-SP/Lemma1 ascent) is a locally-scoped free var rebound per statement; Lemma2 scope (479-497) uses e uniformly for the cross edge, no e_k there. Clean.
6. Bib: KMMS24 added (Klobas,Mertzios,Molter,Spirakis, JCSS 146:103564, 2024). All 3 bibitems present+used (BBMRY13, CDO25 now cited line112, KMMS24 line110). The Round-6 nit "MAL attributed with no \cite" is now RESOLVED (line112 has \cite{CDO25}).
NAMING vs no-age-bound consistency risk (user flagged): problem still called MAL-C (Minimum *Aged* Labelling w/ Capacity) while base is ML with explicitly "no maximum-label bound". This is a NAMING/BRANDING inconsistency ONLY, not a math error — "Aged" in the acronym has no formal counterpart in Problem 2 (no age/max-label constraint exists in prob:malc; objective eq:objective is purely vehicle-dispatch count). Nothing in the algorithm/analysis references an age bound, so no dangling dependency. Recommend author either (a) rename to reflect capacity-not-age, or (b) add one sentence noting the name is inherited from the MAL lineage. Purely editorial.
Worked example: ALL 7 checkpoints recomputed independently again — C_1 D_in=4/D_out=3,(b,c)={0,6},(a,b)=4,(c,a)=1,intra c→b b@6; t_cross=7;T_2=9; C_2 D_in=D_out=2,(q,r)={9,12},(p,q)=11,(r,p)=10; inter a→r monotone r@13, cross→q@9=T_2. Fig labels match. All correct.
Full re-verify (not just diffs): spanning-arborescence fatal fix INTACT (line234-236 induced-SCC-strongly-connected); OPT≥OPT_frac / OPT≥OPT_SF; SP (1+1/θ)OPT_frac; SF F_e-with-multiplicity → ALG_SF≤(4hθ+2)|E_SF|; θ*=1/(√h n^{1/3}); two regimes; common-pitfall hθ*<1 when h<n^{2/3}. All sound. No dangling \ref/\cite (all 45+ refs resolve). VERDICT PASS. Only outstanding item = MAL-C-name-vs-no-age-bound editorial note.

--- (Round 5 detail below) ---
6. Model rewrite: obj(1) first then ⌈·⌉-vehicle explanation; store-and-forward (free node buffering) in model; no max-label; time-0 release; λ:E→2^{Z≥0}, Z≥0={0,1,...}. Self-consistent. Round-3 label-0 clash GONE: only ℕ left is w:E→ℕ (travel times ≥1, line81/145) and MAL Problem1's λ:E→2^ℕ,{1,..,a} (line118 — that's the PRIOR classical problem, different, correct). MAL-C uniformly Z≥0.
Full re-verify: spanning-arborescence fatal fix INTACT (compressed to "C_i strongly connected"+"inside C_i"+"use only edges of G_SF"; induced-SCC-strongly-connected argument implicit but conclusion correct + eq:treerec grounds recurrences). Lower bounds OPT≥OPT_frac (roundup≥flow, each unit≥len(SP)), OPT≥OPT_SF (support⊇DSF) both OK. θ* algebra, two regimes, common-pitfall remark all correct. Thm1 induction (arrival≤in-tree departure since D_in≥d_in(entry)), Thm2 two-label. All 7 worked-ex checkpoints re-recomputed independently = correct. All \ref/\eqref targets exist; def:canonical labeled-but-uncited (harmless, not dangling). Only cite BBMRY13, defined. VERDICT PASS. The F_e-multiplicity notation slip is the single thing worth an author fix (redefine F_e with multiplicity or note it), purely cosmetic to the proof.
